---
# This configuration was generated by
# `rubocop --auto-gen-config`
# on 2022-12-26 16:52:30 UTC using RuboCop version 1.39.0.
# The point is for the user to remove these configuration records
# one by one as the offenses are removed from the code base.
# Note that changes in the inspected code, or installation of new
# versions of RuboCop, may require this file to be generated again.

# Offense count: 1
# Configuration parameters: ExpectMatchingDefinition, CheckDefinitionPathHierarchy, CheckDefinitionPathHierarchyRoots,
# Regex, IgnoreExecutableScripts, AllowedAcronyms.
# CheckDefinitionPathHierarchyRoots: lib, spec, test, src
# AllowedAcronyms: CLI, DSL, ACL, API, ASCII, CPU, CSS, DNS, EOF, GUID, HTML, HTTP, HTTPS, ID, IP, JSON, LHS, QPS, RAM,
# RHS, RPC, SLA, SMTP, SQL, SSH, TCP, TLS, TTL, UDP, UI, UID, UUID, URI, URL, UTF8, VM, XML, XMPP, XSRF, XSS
Naming/FileName:
  Exclude:
    - 'lib/pronto-bigfiles.rb'

# Offense count: 10
# Configuration parameters: AllowSubject.
RSpec/MultipleMemoizedHelpers:
  Exclude:
    - spec/pronto/big_files/message_creator_spec.rb
    - spec/pronto/big_files/patch_inspector_spec.rb
    - spec/pronto/big_files_spec.rb
